Flow of Code or Pseudocode:

Fancy Intro Msg Run()

Initialize keypress listener()
Initialize main thread that runs everything()
  
    while program is enabled()
        while this loop is enabled()
            check user input: {use func_lib.py func}
                if keypress is "E":
                    exit code
                elif keypress is "S":
                    goto settings:
                    